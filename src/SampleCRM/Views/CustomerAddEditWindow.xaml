<ChildWindow x:Class="SampleCRM.Web.Views.CustomerAddEditWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           Title="Add Customer"
           xmlns:local="clr-namespace:SampleCRM.Web.Views">
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:CustomerAddEdit x:Name="customerAddEditView" Grid.Row="0" 
                               CustomerAdded="customerAddEditView_CustomerAdded"
                               CustomerDeleted="customerAddEditView_CustomerDeleted" />
        <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" />
        <Button x:Name="OKButton" Content="Save" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />
    </Grid>
</ChildWindow>