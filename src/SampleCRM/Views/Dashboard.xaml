<local:BasePage x:Class="SampleCRM.Web.Views.Dashboard"
                 xmlns:local="clr-namespace:SampleCRM.Web.Views"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=OpenSilver.Controls.Navigation"
                 mc:Ignorable="d" d:DesignWidth="640" d:DesignHeight="480"
                 Style="{StaticResource PageStyle}">
    <local:BasePage.Resources>
        <local:BinaryImageConverter x:Key="imgConverter" />
        <local:BoolToVisibilityConverter x:Key="boolToVisibilityConverter" />
        <local:DateTimeFormatConverter x:Key="DateTimeFormatConverter" />
    </local:BasePage.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Margin="5">
            <TextBlock Text="Latest Customers" />
            <DataGrid x:Name="grdCustomers" HorizontalContentAlignment="Stretch"
                  ItemsSource="{Binding Customers}" 
                  IsReadOnly="True"
                  AutoGenerateColumns="False" CanUserReorderColumns="True" CanUserSortColumns="True" CanUserResizeColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding CustomerID}" IsReadOnly="True" />
                    <DataGridTemplateColumn Header="Image" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Path=Picture, Converter={StaticResource imgConverter}}" 
                                Width="32" Height="32" Margin="5" Stretch="Fill" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Name" Binding="{Binding FullName}" />
                    <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" />
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>

        <StackPanel Grid.Column="0" Grid.ColumnSpan="1" Grid.Row="1" Margin="5">
            <TextBlock Text="Latest Orders" />
            <DataGrid x:Name="grdOrders"  HorizontalContentAlignment="Stretch"
                  ItemsSource="{Binding Orders}" 
                  IsReadOnly="True"
                  AutoGenerateColumns="False" CanUserReorderColumns="True" CanUserSortColumns="True" CanUserResizeColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order Id" Binding="{Binding OrderID}" />
                    <DataGridTextColumn Header="Customer Id" Binding="{Binding CustomerID}" />
                    <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate, Converter={StaticResource DateTimeFormatConverter}}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.ColumnSpan="1" Grid.Row="1" Margin="5">
            <TextBlock Text="Top Sale Products" />
            <DataGrid x:Name="grdProducts" HorizontalContentAlignment="Stretch"
                  ItemsSource="{Binding Products}" 
                  IsReadOnly="True"
                  AutoGenerateColumns="False" CanUserReorderColumns="True" CanUserSortColumns="True" CanUserResizeColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ProductId" Binding="{Binding ProductID}" />
                    <DataGridTemplateColumn Header="Image" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Path=Picture, Converter={StaticResource imgConverter}}" 
                                Width="32" Height="32" Margin="5" Stretch="Fill" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Price" Binding="{Binding ListPrice}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

    </Grid>
</local:BasePage>
