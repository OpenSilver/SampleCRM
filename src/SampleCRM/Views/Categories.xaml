<local:BasePage x:Class="SampleCRM.Web.Views.Categories"
                 xmlns:local="clr-namespace:SampleCRM.Web.Views"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=OpenSilver.Controls.Navigation"
                 mc:Ignorable="d" d:DesignWidth="640" d:DesignHeight="480"
                 Style="{StaticResource PageStyle}">
    <local:BasePage.Resources>
        <local:BinaryImageConverter x:Key="imgConverter" />
    </local:BasePage.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Categories" Style="{StaticResource HeaderTextStyle}" />

        <DataGrid x:Name="gridCategories"  
                  Grid.Row="1"
                  ItemsSource="{Binding CategoryCollection}" 
                  SelectedItem="{Binding SelectedCategory}"
                  AutoGenerateColumns="False"
                  CanUserReorderColumns="True" 
                  CanUserSortColumns="True" 
                  CanUserResizeColumns="True"
                  RowEditEnded="gridCategories_RowEditEnded">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding CategoryID}" IsReadOnly="True" Width="50" />
                <DataGridTemplateColumn Header="Image" Width="64" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Path=Picture, Converter={StaticResource imgConverter}}" 
                                   Width="64" Height="64" Stretch="Fill" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150" />
                <DataGridTemplateColumn Header="Description" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextWrapping="Wrap" Text="{Binding Description}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox TextWrapping="Wrap" Text="{Binding Description}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <DataForm x:Name="formCategories" 
                  Grid.Row="2"
                  EditEnded="formCategories_EditEnded"
                  AutoGenerateFields="False"
                  AutoEdit="False"
                  AutoCommit="True"
                  CommandButtonsVisibility="Edit, Add, Delete, Commit, Cancel"
                  CommitButtonContent="Save"
                  CancelButtonContent="Cancel"
                  Header="Category Details"
                  ItemsSource="{Binding CategoryCollection}"
                  CurrentItem="{Binding SelectedCategory}">
            <DataForm.EditTemplate>
                <DataTemplate>
                    <StackPanel>
                        <DataField Label="Id">
                            <TextBlock Text="{Binding CategoryID}" />
                        </DataField>
                        <DataField Label="Name">
                            <TextBox Text="{Binding Name}" />
                        </DataField>
                        <DataField Label="Name">
                            <TextBox TextWrapping="Wrap" Text="{Binding Description}" />
                        </DataField>
                    </StackPanel>
                </DataTemplate>
            </DataForm.EditTemplate>
        </DataForm>

        <StackPanel Grid.Row="3" HorizontalAlignment="Left" Orientation="Horizontal">
            <Button Content="Save Changes" Click="SaveButton_Click" Margin="5" x:Name="SaveButton"></Button>
            <Button Content="Reject Changes" Click="RejectButton_Click" Margin="5" x:Name="RejectButton"></Button>
            <TextBlock x:Name="ChangeText" VerticalAlignment="Center" Width="Auto"></TextBlock>
        </StackPanel>

    </Grid>
</local:BasePage>